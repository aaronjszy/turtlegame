{
  "name": "turtlegame",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist/ && git diff --cached --quiet || git commit -m 'deploy: rebuild dist' && git push"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.11"
  }
}
